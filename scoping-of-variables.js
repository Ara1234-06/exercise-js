let outside = 14;
{
  let inside = 27;
  console.log("inside block - outside" + outside);
  console.log("inside block - indide" + inside);
}
console.log("out side block - outside" + outside);
console.log("outsidde block - inside" + typeof inside);
